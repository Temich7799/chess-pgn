export default function checkPGNFormat(pgn: string): boolean {

    // const moveRegex = /^\d+\.\s\S+(\s\S+)*$/;

    // const moves = pgn.split(/\d+\./).filter(Boolean);

    // for (const move of moves) {
    //     if (!moveRegex.test(move.trim())) {
    //         return false;
    //     }
    // } todo

    return true;
}
