export type User = {
    email: string;
    name: string;
    day: string;
    month: string;
    city: string;
    language: string;
    foreign?: string;
    another_foreign?: string;
    note?: string;
};