// import {
//     getConnection
// } from '../../../src/lib/db';

export async function GET(req, res) {

    // const connection = await getConnection();

    // try {
    //     const results = await connection.query('SELECT city FROM users');
    //     return Response.json(results);
    // } catch (error) {
    //     res.status = 500;
    //     return Response.json({
    //         error: error.message,
    //     });
    // } finally {
    //     connection.end();
    // }

    return Response.json({});
}